---
# vars file for ansible-wireguard

# peers_list:

#   - username: '<username>'
#     device: '<device>'
#     wireguard_client_pub: '<generate with: "umask 077; wg genkey > wg-vpn.key && echo -n "Secret Wireguard Key: " | cat - wg-vpn.key">'
#     wireguard_client_key: '<generate with: "wg pubkey < wg-vpn.key > wg-vpn.pub && echo -n "Public Wireguard Key: " | cat - wg-vpn.pub">'
#     wireguard_client_id: <number between 1 and 253>
  
#   # One-liner for keys creation (wireguard required):
#   # umask 077; wg genkey > wg-vpn.key && echo -n "Secret Wireguard Key: " | cat - wg-vpn.key && wg pubkey < wg-vpn.key > wg-vpn.pub && echo -n "Public Wireguard Key: " | cat - wg-vpn.pub
